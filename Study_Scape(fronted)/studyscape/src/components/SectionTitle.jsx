import React from "react";

const SectionTitle = ({ title }) => {
  return (
    <h2 className="text-4xl font-bold text-center mb-10 text-[#ff005c] drop-shadow-lg">
      {title}
    </h2>
  );
};

export default SectionTitle;
